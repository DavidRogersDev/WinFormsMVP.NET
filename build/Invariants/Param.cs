namespace Invariants
{
    public static class Param
    {
        public static class Description
        {
            public const string Configuration = "Configuration to build - Default is 'Debug' (local) or 'Release' (server)";
        }
    }
}
